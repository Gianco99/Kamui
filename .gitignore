# Personal working notes - reference only, not part of the repo
docs/

# Saved edmDumpEventContent output - reference only, regenerable

# Generated job areas, DAS cache, python bytecode
ntupleProduction/jobs/
ntupleProduction/.dasCache/
ntupleSelection/jobs/
ntupleSelection/out/
__pycache__/
*.pyc
*.pyo

# Batch-system leavings. crab writes crab.log into whatever directory it is run from,
# and condor writes .out/.err per job, so these appear at the repo root by accident.
*.log
*.out
*.err
logs/
crab_*/

# Data. Nothing in this repo is an ntuple; anything with these extensions got here by mistake.
*.root
*.tmp

# Editor and shell leavings
typescript
.ipynb_checkpoints/
*.swp
.DS_Store
