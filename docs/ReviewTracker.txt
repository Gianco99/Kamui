Documentation and CLI review tracker
====================================
What has been reviewed against the code, and what has not. Updated as we go. A line moves to Reviewed only after someone has read it against the thing it describes, not merely edited it.


Reviewed, 2026-08-28
--------------------

CLI help output
  Every command's help text and its one-line description, checked against the command table in the root README. Text and registration order match for all thirteen. Verified each command's help renders and that -h still works everywhere.

Root README.md and CLAUDE.md
  Layout, the two-stage description, the quick starts, the command table, the writing rules and the conventions.

The check command
  What it validates, that each check has power, and that it exits non-zero on a real failure. Confirmed by breaking a selection config on purpose and watching it fail. Its output was rewritten to name all nine checks, split them into config and architecture, and mark each with a status. Selection configs are now resolved, which they were not before.

python/kamui/README.md and CLAUDE.md, the check sections only
  The check entry in the README and the check bullets in the CLAUDE.md. The per-command list in the CLAUDE.md was removed as a duplicate of the README.


Edited but not reviewed
-----------------------
These were changed while fixing something else, which is not the same as having been read against the code. They stay unreviewed.

python/kamui/README.md, the stage, submit, select and resubmit sections
  Renamed --dry-run to --dryRun and removed the slimming.py entry, since that module no longer exists.

python/kamui/CLAUDE.md, the submit/ and helpers/ sections
  Same flag rename, plus the banner note, which now only draws for help output.


Next up
-------
The order commands are presented in follows the workflow, so a reader meets each one at the point they would actually need it. Only find and cache work before a sample is registered. query, list and stage all select through the catalog, so they cannot be demonstrated until the sample exists in config/samples/. That fixes the order.

1. find
   Done 2026-08-29. Takes a raw DAS pattern and needs no catalog, so it comes first.
2. Registration
   Done 2026-08-28. config/samples/ README and CLAUDE.md, the tutorial family, subdirectory loading.
3. query
   Done 2026-08-29. Now reports "Not found in DAS" instead of zeros, and exits non-zero, which is what catches a bad dataset path pasted from find.
4. list
   Done 2026-08-31. Flags matched the docs, the first command in this sweep with no stale ones. Fixed a hardcoded plural that printed "1 samples", and a column-width fallback of 4 that did not match the "Sample" header. One README sentence still to correct, see below.
5. stage and cache
   Done 2026-08-31. Flags matched the docs on both. Fixed the [dry-run] label to [dryRun] in seven places across condor.py, crab.py, fetch.py and batch.py, and the file(s)/response(s) plurals.
6. Collections and triggers
   Done 2026-08-31. Removed extension, params and drop, none of which any config used and params was never wired to a producer. Moved dvDisplaced's overrides into the collection defaults, so jets is now era-specific (CHS for Run 2, PUPPI for Run 3) and the track preselection is a default. Verified by resolving all 9 (preset, era, isMC) combinations registered samples use against a rebuilt pre-edit tree: zero changed. Deleted both vetoes blocks from the trigger configs and loadTriggerVetoes, which had no callers. Condensed both CLAUDE.md files.
7. Presets
   Done 2026-09-01. dvLepton now includes dvFull directly instead of descending from dvDisplaced, verified a no-op twice. Slide made.
8. The content command
   Done 2026-09-01. Flags matched the docs. Formatted the raw dict the skim line was printing, added column headers to the table, fixed the collections/variables plurals. Slide made.

   That closes the Defining Our Ntuples section: collections, triggers, presets, content.

9. Submitting jobs
   Next up, and nothing here is reviewed. Commands submit, status and resubmit; config/sites.json; ntupleProduction/ README.md and CLAUDE.md, which keep their pair because cmssw/ holds real code. This section has to run before the selection section can be demonstrated, since there is currently no ntuple on disk that the present configs can read.

10. Applying selections
   The configs are reviewed as of 2026-09-01, the slides are not. Covers config/selections/, select and cutflow together, demonstrated against the ntuples the submission section produces. Held back from the ntuple section deliberately: a cutflow shown abstractly is a schema walkthrough, shown against real output it is the result.

11. Every markdown under config/content and config/triggers, once the above are settled.
12. Generator sums are at 79/144. The 65 that remain are private phys03 productions with no central NanoAOD, tracked in a Git issue rather than here.
13. One final pass over everything once the slides are done, running each command in order to confirm the repo still behaves the way the slides say it does. Functionality changes as the review goes, so this is the checkpoint that catches anything the changes broke.
14. The README.md and CLAUDE.md sitting at the root of each big directory, done at the end once every command under them has been reviewed. That means the root of the repo, python/kamui/, config/, ntupleProduction/, tools/ and docs/. ntupleSelection/ no longer has a pair; its docs were folded into python/kamui/ on 2026-09-01, since the directory holds no code. Reviewing them earlier only means reviewing them twice, since the commands they summarize keep changing.
15. Beta test: have someone who has not seen the repo follow the slides end to end.


Presets, reviewed 2026-08-31, still to act on
---------------------------------------------
Trigger files get almost no validation. validateTriggers confirms only that the file parses and paths is non-empty. There is no unknown-key check, unlike collections and skim blocks, so a misspelled key passes check and does nothing.


Unfinished work
---------------

One wrong sentence in python/kamui/README.md, Picking samples
  It says every command except list exits when nothing matched. On list an unknown --name, --tag or --family does still exit 1; only a --match that matches nothing is tolerated. Needs rewording to scope the exception to --match.

Generator sums verification pass
  Re-measuring all 79 already-measured entries and diffing against a snapshot, to catch any more entries written by the old measure() path. Roughly a third of the way through as of 2026-08-31, zero mismatches beyond the ZH one already fixed. Resume with scratchpad/verify.sh, which restarts from the top and is idempotent.

Generator sums stop at 79/144
  The 65 unfilled are stealthSusy2024 and rpv2024, all on prod/phys03, which have no central NanoAOD for norm to read. Filed as a Git issue. The fix is to read the Runs counters from our own ntuple output, which already writes them through genWeightsTable, and which are valid whenever the production covered the complete input file list.


Known gaps, deliberately not closed
-----------------------------------

Trigger paths are never verified to exist
  check confirms a trigger file parses and lists some paths. Nothing checks that an HLT path was ever in a real menu. A typo passes here, then matches no branch at runtime and silently drops out of the trigger OR.

Nothing keeps the CLI and the root README in step
  The root README command table is the source of the thirteen one-line help strings. Only convention keeps them equal. check could assert it in a few lines.


Not yet reviewed
----------------

python/kamui/README.md and CLAUDE.md, everything except check
  The other twelve commands, and the module sections: foundations/, configReaders/, grid/, submit/, select/, helpers/.

config/
  README.md and CLAUDE.md for config/ itself, and for samples/, content/, triggers/, selections/, normalizations/.

ntupleProduction/
  README.md and CLAUDE.md. It keeps its pair because cmssw/ holds real code.

tools/ and docs/
  README.md and CLAUDE.md.

docs/JetID.txt and docs/GlueballSurvey.txt
  Reference notes, not yet checked against the code they describe.
